/**
   * @type {import('next').NextConfig}
   */
const nextConfig   = {
  images: {
    loader: 'akamai',
    path: '',
  },
  basePath: '/pyreas-com',
  assetPrefix: '/pyreas-com/',
};

export default   nextConfig;
